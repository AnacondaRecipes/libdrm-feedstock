anaconda_config_version: 1
upstream_sources:
  - gitlab:
      identifier: mesa/drm
      host: gitlab.freedesktop.org
      use_max_tag: true
      prefix: libdrm-
      include_regex: ^libdrm-[\d.]+$
